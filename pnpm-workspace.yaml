# pnpm 11: autoriza los build-scripts de estas dependencias para que
# `pnpm install` salga con código 0. Next 16 valida las dependencias (corre
# `pnpm install`) antes de `build`, y pnpm 11 falla si hay scripts sin aprobar.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
